<article>
  {{ if ne .Section "tags" }}
  <time><code>{{ .Date.Format "2006-01-02" }} &rarr;</code></time>
  {{ end }}
  <a href="{{ .Permalink }}">{{ .Title }}</a>
  <!-- {{ range .Params.tags }} -->
  <!-- <a href="{{ "/tags/" | relLangURL }}{{ . | urlize }}">{{ . }}</a> -->
  <!-- {{ end }} -->
</article>
